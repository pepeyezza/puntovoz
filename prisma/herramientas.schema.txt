model HerramientaTecnologica {
  id          String   @id @default(cuid())
  nombre      String
  slug        String   @unique
  descripcion String?
  categoria   String   @default("Otros")
  logoUrl     String?
  enlace      String
  destacada   Boolean  @default(false)
  createdAt   DateTime @default(now())
}
